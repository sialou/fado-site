import { Component } from '@angular/core';
import { FormsModule } from '@angular/forms';
@Component({standalone:true,selector:'app-contact',imports:[FormsModule],template:`<section class="wrap"><h1>Contact</h1><form (ngSubmit)="submit()" #f="ngForm" class="form"><label>Nom<input name="name" [(ngModel)]="form.name" required></label><label>Email<input type="email" name="email" [(ngModel)]="form.email" required></label><label>Message<textarea name="message" [(ngModel)]="form.message" rows="5" required></textarea></label><button class="btn" type="submit">Envoyer</button><p class="muted">Formulaire demonstration — pas de backend configuré.</p></form></section>`,styles:[`.wrap{padding:2rem 1rem}.form{max-width:640px;display:grid;gap:.75rem}input,textarea{padding:.6rem;border:1px solid #e5e7eb;border-radius:.5rem}.btn{background:var(--brand);color:white;padding:.6rem 1rem;border-radius:.5rem;border:1px solid var(--brand)}`]})
export class ContactPageComponent{ form={name:'',email:'',message:''}; submit(){ alert('Merci — message envoyé (démo)'); } }
